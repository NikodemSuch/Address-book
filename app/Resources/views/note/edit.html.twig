{% extends 'default/base.html.twig' %}

{% block page %}

  <div class="container">
    <div class="row">
      <div class="form-container col-sm-6 col-sm-offset-3">

        <div class="form-header">
          <h2 class="form-title">Note edit</h2>
        </div>

        {{ form_start(edit_form) }}
          {% include 'default/formErrors.html.twig' %}

          {{ form_widget(edit_form) }}
          <button class="form-button btn btn-primary btn-lg btn-block" type="submit">Edit</button>
        {{ form_end(edit_form) }}

        <div class="form-footer">
          <a href="{{ path('homepage') }}">Back to the list</a>
        </div>

      </div>
    </div>
  </div>

{% endblock %}
